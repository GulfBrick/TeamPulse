# ─── Database ────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USER=teampulse
DB_PASSWORD=teampulse_dev
DB_NAME=teampulse

# ─── Auth ────────────────────────────────────────────────────
# IMPORTANT: Use a strong random secret in production (32+ chars)
# Generate one with: openssl rand -base64 48
JWT_SECRET=change-this-to-a-long-random-string-in-production
ADMIN_EMAIL=admin@teampulse.local
# IMPORTANT: Use a strong password (8+ chars) in production
ADMIN_PASSWORD=admin123

# ─── Server ──────────────────────────────────────────────────
PORT=8080

# ─── CORS ────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Leave unset for * (dev only).
# Example: CORS_ORIGINS=https://teampulse.yourcompany.com,https://admin.yourcompany.com
CORS_ORIGINS=
